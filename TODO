#Combine the elements into one script
Make the scan processing quicker by defaulting to scanning +-20 pixels from previous detected point
Comment code
Test on RasPi
Add capability to trigger camera through existing software such as gphoto

Use GPIO pins to rotate platter
